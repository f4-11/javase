package base;
//ctrl+alt+l 代码格式化, alt+insert @override重写， 多态是方法的多态父类引用指向子类对象，
//static/final/private不能重写
import java.util.Arrays;
import java.util.Scanner;

//访问权限上，public跨类跨包 > protected所有包的子类 > default本包 > private本类
//继承关系 ctrl + h, java只有单继承, 先执行父类构造，再执行子类构造
//嵌套类static class可以访问private
public class HelloWorld {
//    public static int add(int a, int... b) {
//        for (int x:b){
//            a+=x;
//        }
//        return a;
//    }
//
//    public static void main(String[] args) { //psvm
        /*
        javadoc可以生成类的文档
        变量命名规范：
        类成员变量，局部变量,方法名：monthSalary
        常量：MAX_VALUE
        类名：GoodMan
         */
        //shift单击下面的可以一次性选中多个，鼠标拖动进package
//        System.out.println("hello world"); //sout
//        System.out.println(add(1,2,3,4)); //ctrl+d复制上一行,ctrl+y删除整行，>>>逻辑右移
//        Scanner scanner = new Scanner(System.in);//alt+enter自动补全左面
//        System.out.println("输入：");
//        if(scanner.hasNext()){
//            String str = scanner.next();
//            System.out.println("输出了"+str);
//        }
//        if(scanner.hasNextLine()){
//            String str = scanner.nextLine();
//            System.out.println("output:"+str);
//        }
//        if(scanner.hasNextInt()){
//            int i = scanner.hasNextInt();
//        }

        //for循环可以使用同时使用多个变量

        /*
        Arrays类
        int[]a = {1,2};
        Arrays.toString(a);
        a.length //2
        Arrays.sort(a);
         */
//    }
}
